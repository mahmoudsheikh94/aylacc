[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"

# Redirects for SPA-like behavior (optional, Astro generates static HTML)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 404
