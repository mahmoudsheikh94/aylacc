---
import { services } from '../data/services';

const icons: Record<string, string> = {
  building: `<path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4" />`,
  home: `<path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6" />`,
  layout: `<path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M4 5a1 1 0 011-1h14a1 1 0 011 1v2a1 1 0 01-1 1H5a1 1 0 01-1-1V5zM4 13a1 1 0 011-1h6a1 1 0 011 1v6a1 1 0 01-1 1H5a1 1 0 01-1-1v-6zM16 13a1 1 0 011-1h2a1 1 0 011 1v6a1 1 0 01-1 1h-2a1 1 0 01-1-1v-6z" />`,
  refresh: `<path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15" />`,
  stairs: `<path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M3 21h6v-6h6v-6h6V3" />`,
  expand: `<path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M4 8V4m0 0h4M4 4l5 5m11-1V4m0 0h-4m4 0l-5 5M4 16v4m0 0h4m-4 0l5-5m11 5l-5-5m5 5v-4m0 4h-4" />`,
};
---

<section id="services" class="section-padding relative overflow-hidden section-bg-light">
  <div class="relative container-professional">
    <!-- Section Header -->
    <div class="text-center max-w-3xl mx-auto mb-16 lg:mb-20">
      <div class="inline-flex items-center gap-3 mb-5">
        <div class="blue-line"></div>
        <span class="text-eyebrow">What We Offer</span>
      </div>

      <h2 class="heading-lg mb-5">
        Our <span class="text-gradient">Services</span>
      </h2>

      <p class="text-large">
        Comprehensive construction solutions tailored to your vision.
        From concept to completion, we deliver excellence.
      </p>
    </div>

    <!-- Services Grid -->
    <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-6 lg:gap-8">
      {services.map((service, index) => (
        <article
          class="group card-professional p-8 lg:p-10 hover:shadow-lg"
          style={`animation-delay: ${index * 0.1}s`}
        >
          <!-- Number & Icon row -->
          <div class="flex items-start justify-between mb-6">
            <div class="w-14 h-14 bg-accent-light border-2 border-accent rounded-lg flex items-center justify-center
                        group-hover:bg-primary group-hover:border-primary transition-all duration-300">
              <svg
                class="w-7 h-7 text-primary group-hover:text-white transition-colors duration-300"
                fill="none"
                stroke="currentColor"
                viewBox="0 0 24 24"
                aria-hidden="true"
                set:html={icons[service.icon]}
              />
            </div>
            <span class="font-display text-5xl font-bold text-gray-200 group-hover:text-accent transition-colors duration-300">
              0{index + 1}
            </span>
          </div>

          <!-- Content -->
          <h3 class="heading-sm mb-4 group-hover:text-secondary transition-colors duration-300">
            {service.title}
          </h3>

          <p class="text-text-gray mb-6 leading-relaxed">
            {service.description}
          </p>

          <!-- Features -->
          <ul class="space-y-3 mb-8">
            {service.features.slice(0, 3).map((feature) => (
              <li class="flex items-center gap-3 text-sm text-text-gray">
                <span class="w-1.5 h-1.5 bg-primary rounded-full flex-shrink-0"></span>
                {feature}
              </li>
            ))}
          </ul>

          <!-- CTA -->
          <a
            href="#contact"
            class="inline-flex items-center gap-2 text-primary text-sm font-semibold tracking-wide
                   group-hover:gap-3 transition-all duration-300 hover-line"
          >
            <span>Learn More</span>
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3" />
            </svg>
          </a>
        </article>
      ))}
    </div>

    <!-- Bottom CTA -->
    <div class="mt-16 lg:mt-20 text-center">
      <div class="inline-block card-accent p-8 lg:p-12 rounded-xl max-w-2xl">
        <p class="text-text-dark text-lg font-medium mb-6">
          Not sure which service suits your project?
          <span class="text-primary font-semibold block mt-1">Let's discuss your vision.</span>
        </p>
        <a href="#contact" class="btn-primary">
          <span>Schedule a Consultation</span>
          <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z" />
          </svg>
        </a>
      </div>
    </div>
  </div>
</section>
